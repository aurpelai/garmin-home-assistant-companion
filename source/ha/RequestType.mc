import Toybox.Lang;

// Who a spent failure accuses. A rejection while registering accuses our own
// registration body; the same code on anything else means Home Assistant could
// not do what we asked.
class RequestType {
    static const REGISTRATION = :registration;
    static const REQUEST = :request;
}
