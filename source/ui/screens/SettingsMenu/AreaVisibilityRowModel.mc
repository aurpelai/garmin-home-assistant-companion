import Toybox.Lang;

class AreaVisibilityRowModel extends VisibilityRowModel {

    function initialize(id as String, name as String, isVisible as Boolean) {
        VisibilityRowModel.initialize(id, name, isVisible);
    }
}
