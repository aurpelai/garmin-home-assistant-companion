import Toybox.Lang;

// An entity model Home Assistant reports into an area, named as one type so a
// walk over any of them can share a body. Each declares the areaId grouping reads.
typedef EntityModel as LightModel or SensorModel;
